# Accept user queries (e.g., “Tell me about your Python experience”).

# Use the retriever function from vector_store.py to find relevant resume sections.

# Pass the retrieved information to OpenAI’s GPT API for coherent response generation.

# Format and return the final response to the user.

# Ensure responses stay factual (so AI doesn’t hallucinate false details).
